services:
  - type: web
    name: twindrop-signaling
    runtime: node
    plan: free
    rootDir: .
    buildCommand: npm ci
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: TWINDROP_LOG_LEVEL
        value: info
